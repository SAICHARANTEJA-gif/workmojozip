# Backend Environment Variables (Render / Host)
PORT=5000

# Optional: AI Provider Keys (If left unset, WorkMojo's Built-in Multilingual Domain Engine handles all queries)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional: Supabase PostgreSQL credentials if connecting to live cloud database
SUPABASE_URL=
SUPABASE_ANON_KEY=

